# Place Google credential files here:
#   service_account.json   (GOOGLE_AUTH_MODE=service_account)
#   oauth_client.json + oauth_token.json   (GOOGLE_AUTH_MODE=oauth)
# This directory is mounted read-only into the app container at /secrets.
